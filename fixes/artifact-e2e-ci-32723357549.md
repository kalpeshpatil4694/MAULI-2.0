# CI failure analysis — run 32723357549

The Full L1 validation run reached `npm test` successfully. Node syntax and dependency installation passed. 123/125 tests passed; exactly two failures were isolated and fixed in commit `8f03395f7480d32f2ab005549186730fe19db274`.

1. Artifact E2E parser returned null because Task 3 used `create task-3-output.txt containing exactly ...`, while the parser only recognized `file` / `artifact file` prefixes. The parser now recognizes `create` as well.
2. Malformed AI output fell through to the broad deterministic planner and claimed five capabilities. The safe contract now returns zero execution capabilities for malformed model output.
