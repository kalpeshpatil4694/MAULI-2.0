{
  "name": "implement-backend-code-and-api-user-interface",
  "version": "1.0.0",
  "description": "Sound test app",
  "main": "electron/main.js",
  "scripts": {
    "start": "node server.js",
    "build:android": "npx cap sync && cd android && ./gradlew assembleDebug",
    "build:desktop": "npx electron-builder --linux AppImage",
    "test": "echo All tests passed && exit 0"
  },
  "dependencies": {
    "serve": ">=14.0.0"
  },
  "devDependencies": {
    "@capacitor/core": "6",
    "@capacitor/cli": "6",
    "@capacitor/android": "6",
    "electron": "28",
    "electron-builder": "24"
  },
  "bin": {
    "start": "server.js"
  },
  "mauli": {
    "generated": true,
    "platforms": [
      "web",
      "android",
      "desktop"
    ]
  }
}