name: MAULI L1 Tests

# Manual regression workflow. Automatic L1 validation is owned by l1-ci.yml.
on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: mauli-manual-tests
  cancel-in-progress: true

jobs:
  test:
    name: L1 regression tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 22
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Run L1 tests
        run: npm test
