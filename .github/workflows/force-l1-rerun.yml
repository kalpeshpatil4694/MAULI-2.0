name: Force L1 CI Rerun

on: [push, workflow_dispatch]

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Trigger marker
        run: echo "L1 CI rerun trigger"
